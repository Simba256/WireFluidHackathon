# Project Tracker

## Project Summary

BoundaryLine — a free-to-play fantasy PSL game on WireFluid where players pick teams, earn points from real match performance, and claim real-world prizes via on-chain soulbound trophy NFTs. Built for the WireFluid Hackathon (2026-04-13 → 2026-04-14).

## Current Status

**Status**: Active — core app pages are shipped; demo run, deployment, and presentation polish remain

## In Progress

- None

## Recently Completed

- [x] Added a dedicated static `/banner` marketing route that renders a wide promo composition with BoundaryLine branding, player showcase cards, leaderboard/trophy callouts, and export-friendly framing for sharing outside the app shell. Verification: `pnpm --filter @boundaryline/web typecheck` and `pnpm --filter @boundaryline/web build` both pass — (2026-04-16)
- [x] Perf 13 — `/play` child components share react-query cache. `PlayDispatch` paints instantly from the warmed `playCurrent` cache, but `TeamPickerClient` and `MatchScorecardClient` were still firing their own `useEffect + apiFetch` on every mount, so the page "loaded from scratch" visually. Added `selectedTeam(matchId, wallet)` and `matchScorecard(matchId, wallet)` keys/fetchers, migrated both child components to `useQuery`, and wired `PlayDispatch` to chain-prefetch the right child query the moment the parent `playCurrent` response arrives. Now the first visit warms both the match shell and its child payload, so a second visit paints the full scorecard/picker from cache with a silent background revalidate. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Perf 12 — `/play` served from react-query cache. The `/play` route was still an RSC with `force-dynamic`, so every nav paid a full server round-trip (match lookup + players query) and could never be prefetched. Added `GET /api/play/current` which returns `{ match, players }` in one round-trip using the same priority CTE (`explicit id → live → scheduled`). Replaced the RSC body with a thin wrapper around a new client `PlayDispatch` that reads from `useQuery` and routes to `MatchScorecardClient` or `TeamPickerClient` based on the cached payload. Added `playCurrent` to the shared query keys/fetchers and included it in `prefetchPublic()` so landing-page visits warm the play route before the user clicks it. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Perf 11 — Leaderboard + prize catalog into prefetcher. `components/leaderboard-page.tsx` migrated from its local parallel-fetch + backoff-poll pattern to two `useQuery` hooks keyed on `leaderboardGlobal` / `leaderboardPrize` (with `refetchInterval: 10_000` only while the prize tab is active), so it shares the same cache the prefetcher fills. Added a `prizeCatalog` key + fetcher for `/api/prizes` and migrated `prizes-page.tsx` to pull the catalog via `useQuery` too. Split `prefetchAuthenticated()` into `prefetchPublic()` (leaderboards + prize catalog, unconditional) and the user-scoped bundle, so the landing page warms rankings + prizes even for visitors without a wallet session. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Perf 8-10 — Background prefetch + react-query SWR on authenticated pages. Tuned the existing `QueryClient` defaults (`staleTime: 30s`, `gcTime: 5min`, `refetchOnWindowFocus: false`). Added `apps/web/lib/queries.ts` with centralized query keys + fetchers for wallet balance, `/api/dashboard/me`, `/api/dashboard/chain-state`, `/api/dashboard/global-standing`, leaderboard global/prize, and trophies, plus a `prefetchAuthenticated()` helper. New `AppPrefetcher` component mounted on both the landing page (`app/page.tsx`) and the `(app)/layout.tsx` fires `queryClient.prefetchQuery()` for every authenticated endpoint the moment a wallet session exists, so navigating to dashboard/prizes/trophies paints instantly from the warmed cache while react-query revalidates in the background. Migrated `components/trophies-page.tsx`, `components/prizes-page.tsx`, and `components/dashboard-page.tsx` from `useEffect + apiFetch` to `useQuery` so they share state with the prefetcher; post-mutation refreshes now go through `invalidateUserQueries()` instead of rerunning imperative loaders. Verification: `pnpm --filter @boundaryline/web typecheck` + `pnpm --filter @boundaryline/web build` both pass — (2026-04-15)
- [x] Perf 7 — Consolidate `/play` match lookup into a single query. `apps/web/app/(app)/play/page.tsx` previously ran up to three sequential SELECTs to find the "match to show" (explicit id → live → scheduled). Collapsed into one query with a `CASE`-based priority `ORDER BY` so one round-trip picks the explicit id first, then any live match, then the next scheduled match. Same behavior, one DB hop instead of three. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Perf 6 — Cache `/api/dashboard/me` + bound activity queries. 10s per-wallet in-memory cache on the dashboard summary route; the previously sequential tournament + user lookups were folded into the main `Promise.all` batch alongside pointRow/teamRow/currentClaim/pendingSync for one less round-trip; added `LIMIT 10` on the live-matches query (was unbounded). `invalidateDashboardMeCache(wallet)` is called from `/api/sync/confirm`, `/api/claim/status` reconciliation, and `POST /api/teams` so user-facing writes invalidate the cache immediately. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Perf 5 — Leaderboard parallel fetch + exponential backoff. `leaderboard-page.tsx` now fetches global + prize leaderboards in parallel on mount so tab switches are instant instead of paying a fresh round-trip. The prize-tab background refresh replaced the fixed 5s poll with a 5→10→20→30s exponential backoff that resets whenever `updatedAt` changes, using a ref-tracked snapshot comparison to skip `setState` (and the resulting render churn) when the payload is unchanged. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Perf 4 — Trophies route cache + skip chain scan when reconciled. `apps/web/app/api/trophies/[wallet]/route.ts` now caches the full response per wallet for 60s in an in-memory map, skips the `getContractEvents` scan entirely when no pending/expired claims exist for the wallet (the common case post-reconcile), and parallelizes the reconcile UPDATEs via `Promise.all`. Cache is busted from `/api/claim/status` when lazy reconciliation patches a row, and exposed via `invalidateTrophiesCache(wallet)`. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Perf 3 — Seed auth profile from verify response + localStorage. `auth-provider.tsx` now persists `{username, avatarUrl}` into the stored session on SIWE verify and restores it on mount, so protected pages no longer block on a second `/api/auth/me` round-trip after reload. `setUsername` and `updateAvatar` also write through to the stored session so the seed stays fresh. The blocking `/api/auth/me` useEffect is now a no-op when profile is already seeded. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Perf 2 — Lightweight header wallet balance. AppHeader was pulling the full `/api/dashboard/chain-state` payload (which rebuilt the prize leaderboard) just to display the wallet balance pill. Added a new `GET /api/dashboard/wallet-balance` route that does a single `readContract(balanceOf)` and returns just the balance, and pointed `app-header.tsx` at it. Dashboard + prizes pages still use the full chain-state payload, which is now fast thanks to Perf 1's cache. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Perf 1 — Cached + parallelized prize leaderboard state. `apps/web/lib/prize-state.ts` now memoizes `getPrizeLeaderboardState()` with a 30s in-memory TTL keyed on `(tournamentId, includeWallets)`, and the per-wallet chain reads flipped from the batch-of-20 sequential loop to a single `Promise.all` over all wallets. Cache is explicitly busted via new `invalidatePrizeStateCache()` from `apps/web/app/api/sync/confirm/route.ts` on successful confirm and from `apps/web/app/api/claim/status/route.ts` when lazy-reconciliation patches a pending claim, so leaderboard staleness is bounded to 30s idle + instant on state change. Plan documented in `docs/PERF_PLAN.md`. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Fixed inverted tier → trophy NFT mapping. `PSLTrophies.sol` was deployed with `tierNames` in the reverse order of the backend `TierId` enum, so claiming Top 3 minted a "Top 25 Finisher" trophy. Rather than redeploy (both contracts — `setTrophies` is one-shot on `PSLPoints`), added `toContractTierId` / `fromContractTierId` helpers in `packages/shared/src/constants.ts` (`contractTierId = 6 - backendTierId`, a self-inverse). Applied the flip at the voucher boundary in `apps/web/app/api/claim/route.ts` when signing `ClaimVoucher`, and the inverse in `apps/web/app/api/claim/status/route.ts` and `apps/web/app/api/trophies/[wallet]/route.ts` when reconciling `TierClaimed` events back into the `claim` table. Documented the mapping and the two conversion sites in `docs/CONTRACTS.md` under a new "Tier ID Mapping" section. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Added a demo-only match simulator — new `POST /api/matches/[id]/play` route generates deterministic synthetic stats for every player on both franchises (seeded by `matchId+playerId` so reruns are consistent), computes per-player points via the existing `calculatePlayerPoints()`, credits every user who submitted a `selected_team` lineup for the match based on their 11 picks, writes `team_a_score`/`team_b_score` scoreline strings, and flips the match to `completed` in a single transaction. Wired a `Play Match (Demo)` button into `team-picker-client.tsx` that appears after a squad is saved; clicking it calls the route and `router.refresh()`s so the page RSC re-renders into the existing `MatchScorecardClient`. Verification: `pnpm --filter @boundaryline/web typecheck` passes — (2026-04-15)
- [x] Tightened the `/play?matchId=X` scorecard hero by keeping the matchup short codes only in the center wordmark and lowering the side score blocks, which removes duplicate labels and gives the scoreline more breathing room under the header. Verification: `pnpm --filter @boundaryline/web typecheck` and `pnpm --filter @boundaryline/web build` both pass — (2026-04-15)
- [x] Revamped the `/play?matchId=X` match scorecard into a broadcast-style match detail page focused on squad performance — rebuilt `MatchScorecardClient` with a stadium hero, stronger team score presentation, ranked top-performer feature cards, real-data-only squad insight panels, and a cleaner full scorecard layout that still supports `All Players` / `Your Squad`. Verification: `pnpm --filter @boundaryline/web typecheck` and `pnpm --filter @boundaryline/web build` both pass — (2026-04-15)
- [x] Unified the authenticated shell background color by switching `(app)/layout.tsx` from `bg-background` to `bg-surface-container-low`, which removes the black strip behind page-level headers like `/play` and keeps the header/sidebar/content gutter on the same surface. Verification: `pnpm --filter @boundaryline/web typecheck` and `pnpm --filter @boundaryline/web build` both pass — (2026-04-15)
- [x] Removed the duplicate `BoundaryLine` branding from the authenticated shell by dropping the desktop sidebar wordmark and starting the sidebar below the fixed top header, so the app name now appears only once in the header bar. Verification: `pnpm --filter @boundaryline/web typecheck` and `pnpm --filter @boundaryline/web build` both pass — (2026-04-15)
- [x] Removed the `2 matches` counter from the `/dashboard` match-activity header so that row now only shows the section title and the `VIEW ALL` action, without repeating information the feed already makes obvious. Verification: `pnpm --filter @boundaryline/web typecheck` and `pnpm --filter @boundaryline/web build` both pass — (2026-04-15)
- [x] Removed the `OFF-CHAIN` pill from the `/dashboard` hero metric so the total-points header reads cleaner without a redundant badge next to the main label. Verification: `pnpm --filter @boundaryline/web typecheck` and `pnpm --filter @boundaryline/web build` both pass — (2026-04-15)
- [x] Moved prize stock enforcement from shared constants to the database — `apps/web/lib/prize-state.ts` now reads `prize.stock_limit` per tier when computing `canClaim`, so claimability matches the seeded prize catalog instead of the old hardcoded `1/3/10/25/50` constants. Normalized live Neon prize rows to `stock_limit = 1000` for every tier and removed the unused shared `stockLimit` fields from `TierDefinition`. Verification: `pnpm --filter @boundaryline/shared typecheck`, `pnpm --filter @boundaryline/web typecheck`, and local `GET /api/leaderboard/prize` now shows rank 1 as claimable again — (2026-04-15)
- [x] Removed redundant `/leaderboard` chrome that restated information already visible in the table — deleted the footer tier legend strip and the `Sorted By` stats cards, then collapsed the summary bar to a two-card layout (`Total Players` + `Last Updated` for global, `Qualified` + `Block` for prize). Verification: `pnpm --filter @boundaryline/web typecheck` and `pnpm --filter @boundaryline/web build` both pass — (2026-04-15)
- [x] Renamed the prize-standing row label from `Prize Status` to `Status`, which keeps the card copy shorter and avoids repeating the prize context already established by the card heading. Verification: `pnpm --filter @boundaryline/web typecheck` and `pnpm --filter @boundaryline/web build` both pass — (2026-04-15)
- [x] Centered the dashboard leaderboard CTA and shortened its copy to `View Leaderboard`, so the footer action in the global standings card reads cleaner and no longer hugs the right edge. Verification: `pnpm --filter @boundaryline/web typecheck` and `pnpm --filter @boundaryline/web build` both pass — (2026-04-15)

## Upcoming / Planned

- [ ] `/admin` key auth + score entry flow so match scoring no longer depends on direct backend calls — P0
- [ ] End-to-end demo run + populate `docs/DEMO_TRANSACTIONS.md` — P0
- [ ] Vercel link + env vars + prod deploy — P0
- [ ] Demo video + pitch deck + README polish — P0

## Blockers

- Public Vercel URL `https://wire-fluid-hackathon-web.vercel.app` is serving a pre-dashboard build; `/dashboard` returns `404` until the app is redeployed from current `main`

## Key Decisions

- (2026-04-14, 00:40 PKT — supersedes the 23:15 two-tier decision) **Single 10k earned threshold for both leaderboard visibility and prize claim** — collapsed the two-tier design (1k visibility, 10k claim) into one. The leaderboard ranks by `balanceOf DESC` filtered to wallets with `earnedBalance ≥ 10,000 BNDY`, and `PSLPoints.claimTier()` enforces the same 10k threshold on-chain. Rationale: one number is simpler to pitch, simpler to maintain (one constant, no divergence risk), and has the same anti-whale guarantee. The "visible but not claimable" intermediate state was a nice-to-have onboarding detail; engagement for players under 10k is handled by the global leaderboard (inclusive, off-chain). No contract redeploy needed — the deployed `MIN_EARNED_TO_CLAIM` constant already is 10k. The backend filter reads from `packages/shared/constants.ts` (`MIN_EARNED_TO_CLAIM_WEI`).
- (2026-04-13, 23:15 PKT — superseded by the 2026-04-14 entry above) **Rank by `balanceOf`, qualify by `earnedBalance ≥ 1k`, claim by `earnedBalance ≥ 10k`** — two-tier gating with a soft 1k visibility floor and a strict 10k claim gate. Replaced with a single 10k threshold the next session.
- (2026-04-13, 23:15 PKT) **No indexer daemon, lazy-refresh leaderboard** — Vercel Hobby crons are daily-only (confirmed from Vercel docs). Prize leaderboard refreshes inside the `GET /api/leaderboard/prize` handler when the snapshot is >30s stale: multicall over tracked wallets + Transfer-log scan + snapshot upsert. Client polls every 5s. Railway/Fly indexer is a v2 upgrade, not v1. Keeps single-deploy Vercel architecture.
- (2026-04-13) **Transferable ERC-20 with `earnedBalance` tracking** — rationale: user wanted tradability and secondary market. Original decision ranked by `earnedBalance`; superseded above by the balance-ranked model.
- (2026-04-13) **Soulbound trophy NFTs** — rationale: trophies represent achievement proof, must not be buyable/tradeable, otherwise anyone could fake "Top 10 Finisher" status.
- (2026-04-13) **Dual leaderboard (global off-chain + prize on-chain)** — rationale: off-chain for engagement/inclusivity, on-chain for authoritative prize distribution. Only prize leaderboard determines winners.
- (2026-04-13) **Minimum 10,000 BNDY earned to claim any prize** — rationale: raises cost of Sybil/low-effort claim attacks, forces meaningful gameplay.
- (2026-04-13) **One claim per user per tournament, current-tier-only** — rationale: creates press-your-luck strategic tension; prevents downgrade exploits.
- (2026-04-13) **Off-chain point accumulation, on-chain sync opt-in** — rationale: zero gas for gameplay, on-chain only where trust/prizes require it.
- (2026-04-13) **Free-to-play, gas-only costs** — rationale: no entry fees, avoids gambling classification, reduces legal/regulatory risk.

## Notes

- WireFluid Testnet: Chain ID `92533`, RPC `https://evm.wirefluid.com`, Faucet `https://faucet.wirefluid.com`, Explorer `https://wirefluidscan.com`
- Hackathon scope: 2 days, 3 devs, solo-track deployable demo
- Prizes for demo are mocked (representative) — real partnerships are v2
- v2 roadmap items recorded in `docs/ROADMAP.md`
